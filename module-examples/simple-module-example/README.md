# Quick Notes module example

This is a SpentaCRM Module Runtime v3 example. `spenta-module.json` is the manifest; `dist/` contains the browser-ready runtime surfaces installed from the ZIP. `src/` is optional authoring source.
